# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'apns_with_fcm_pushy' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for apns_with_fcm_pushy
  pod 'Firebase/Messaging'
  pod ‘Firebase’

  target 'apns_with_fcm_pushyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'apns_with_fcm_pushyUITests' do
    # Pods for testing
  end

end
